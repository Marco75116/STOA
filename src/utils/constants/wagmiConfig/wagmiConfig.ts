import { abiDiamond } from "../abi/Diamond";
import { addressDiamond } from "../address/Diamond";

export const DiamondContract = {
  address: addressDiamond as `0x${string}`,
  abi: abiDiamond as any,
};
